import React from "react";
import { observer } from "mobx-react";
import "./styles.css";

class TweetTrending extends React.Component {
    render() {
        return (
            <div>
                vbnvbn
            </div>
        );
    }
}

export default observer(TweetTrending);
